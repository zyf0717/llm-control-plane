# Graph metadata

Optional YAML metadata for LangGraph graphs lives here. Each file is keyed by graph id,
for example `research_agent.yaml`, and is joined with the graph refs in
`langgraph.json`.

The shipped metadata files mirror the built-in graph catalog registered in
`langgraph.json`.
